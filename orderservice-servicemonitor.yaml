apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: orderservice-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: orderservice
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scheme: http
