apiVersion: v1
kind: Service
metadata:
  name: orderservice
  labels:
    app: orderservice 
spec:
  type: LoadBalancer
  selector:
    app: orderservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3200
